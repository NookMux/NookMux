# cmd/AGENTS.md

`cmd/` 存放项目可执行二进制命令入口。当前唯一入口命令是 `server`，详细规则见 [server/AGENTS.md](server/AGENTS.md)。

## 规则

- 命令入口保持极薄封装，仅负责退出码处理与顶层分发，严禁承载业务逻辑、资源初始化或路由装配。
- 新增命令子目录必须配置独立的 `AGENTS.md`，并声明精确的构建与验证指令。

## 验证

- `go build ./cmd/...`
- 针对具体命令的验证见其专属子包规则。
