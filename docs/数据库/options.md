# `options` 表

`options` 保存系统运行配置和一次性数据迁移标记。所有值最终以字符串形式存储，启动时再按配置键解析为布尔、数字、字符串或 JSON。

| 字段 | 类型/存储 | 存放的数据 |
|---|---|---|
| `key` | `string`，主键 | 配置键或迁移标记键。迁移标记格式为 `data_migration.<名称>.v<版本>.done`。 |
| `value` | `string` | 配置值或迁移标记值。普通配置可能是 `"true"`、数字或 JSON 文本；迁移完成后标记值为 `"true"`。 |
