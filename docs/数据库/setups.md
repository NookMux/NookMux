# `setups` 表

`setups` 是系统初始化完成标记。初始化事务会先插入固定主键记录占位，避免并发启动产生重复 root 用户或重复初始化。

| 字段 | 类型/存储 | 存放的数据 |
|---|---|---|
| `id` | `unsigned int`，主键 | 初始化标记 ID；初始化流程使用固定主键做并发占位。 |
| `version` | `varchar(50)` | 完成初始化时的系统版本。 |
| `initialized_at` | `bigint`，Unix 秒 | 系统初始化完成时间。 |
