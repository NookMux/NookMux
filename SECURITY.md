# 安全政策

## 支持的版本

| 版本 | 支持状态 |
| --- | --- |
| 最新版本 | ✅ 提供安全更新 |
| 开发版本 | ⚠️ 仅限测试 |

## 报告安全漏洞

如果您发现了安全漏洞，请**不要**通过公开的 GitHub Issues 报告，请通过以下方式私下联系我们：

### 联系方式

- **邮箱**: <NookMux@nookmux.com>
- **主题**: [SECURITY] NookMux 安全漏洞报告

### 报告内容

请在报告中包含以下信息：

1. **漏洞描述**: 详细描述发现的安全问题
2. **影响范围**: 说明漏洞可能造成的影响
3. **重现步骤**: 提供详细的重现步骤
4. **环境信息**: 包括版本、平台、配置等
5. **建议修复**: 如果有修复建议请一并提供

### 响应时间

- **确认收到**: 24 小时内
- **初步评估**: 72 小时内
- **详细分析**: 7 天内
- **修复发布**: 根据严重程度，通常在 14-30 天内

对于严重安全问题，可将邮件主题标为 `[URGENT SECURITY]`，我们会在收到后 12 小时内优先响应。

## 安全特性

本项目在以下层面内置了安全机制：

- **SSRF 防护**: 渠道外网请求（中继、测试、模型拉取、余额查询等）经过 URL 校验与 SSRF 复查，支持通过渠道配置的代理出网（`internal/infra/security`、`internal/infra/httpclient`）。
- **可信代理**: 支持 `TRUSTED_PROXIES` 环境变量配置可信代理，正确解析客户端真实 IP。
- **认证与会话**: 访问令牌、会话失效重校验等中间件（`internal/httpapi/middleware`）。
- **2FA**: TOTP 两步验证与备份码（哈希存储）。
- **密码存储**: 单向哈希加盐存储，不明文保存密码。
- **限速**: 全局与模型级限速、邮箱验证频控等中间件。
- **跳转校验**: 对重定向 URL 进行合法性校验，防开放重定向。

## 安全最佳实践

### 部署安全

1. **敏感配置**: API key、数据库 DSN、SMTP、OAuth 密钥等一律通过环境变量注入，不写入源码、日志或公开配置。
2. **最小权限**: 数据库、Redis 账号使用最小权限；服务不要以 root 运行。
3. **访问控制**: 管理后台建议置于内网或反向代理访问控制之后；正确配置可信代理，避免 IP 伪造绕过限速。
4. **传输安全**: 生产环境通过 HTTPS 对外提供服务。
5. **定期更新**: 及时更新依赖和运行时，关注 GitHub Releases 获取安全更新。

### 使用安全

1. **令牌管理**: 定期轮换 API 令牌，遵循最小授权原则设置令牌额度和分组。
2. **日志审计**: 关注管理员操作审计日志与异常请求；日志中不应出现密钥等敏感信息。
3. **备份恢复**: 定期备份数据库，并验证备份可恢复。

## 安全贡献

我们欢迎负责任的安全研究（代码审计、渗透测试、漏洞发现、安全改进建议）。对于报告安全问题的研究人员，我们将在适当的地方公开致谢（除非要求匿名）。

---

感谢您帮助保持 NookMux 的安全性！
